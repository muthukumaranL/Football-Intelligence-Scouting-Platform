# Football Intelligence Scouting Platform — runtime dependencies
# Tested with the versions in parentheses; the lower bounds keep the project
# deployable on Streamlit Cloud / Render / Hugging Face Spaces.

streamlit>=1.49          # dashboard framework        (tested 1.58.0)
pandas>=2.1              # data processing            (tested 3.0.2)
numpy>=1.26              # numerical computing        (tested 2.4.4)
scikit-learn>=1.3        # ML models & metrics        (tested 1.8.0)
plotly>=5.18             # interactive charts         (tested 6.8.0)
joblib>=1.3              # model persistence          (tested 1.5.3)
pyarrow>=14.0            # parquet artifact caching   (tested 22.0.0)
